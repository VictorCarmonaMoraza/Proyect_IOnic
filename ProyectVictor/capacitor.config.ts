import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ProyectVictor',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
